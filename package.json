{
  "name": "project1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "on-chrome": "npx cypress run --browser chrome --headed",
    "test": "npm install && npm-run-all on-chrome --continue -on-error"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^11.0.0",
    "npm-run-all": "^4.1.5",
    "cypress-xpath": "^2.0.1"
  }
}
